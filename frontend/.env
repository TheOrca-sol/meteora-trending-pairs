# API URL (optional, defaults to http://localhost:5000/api)
# REACT_APP_API_URL=http://localhost:5000/api

# Note: REACT_APP_ENABLE_LOCALHOST_FEATURES and REACT_APP_ENABLE_DEGEN_MODE
# are no longer needed - all features are now enabled by default in production
